# Instructions
The simulator has been programmed in Python and is targeting version 3.7.
The code is self-contained: no additional package is required.

# Usage
Uncomment the progam you want to run in `run.py`, and then execute `python run.py` from the same directory.